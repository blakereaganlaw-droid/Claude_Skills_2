# Your board conventions (sanitized template)

- **Standing goal statements:** <project/module → its deliverable goals and accuracy
  requirements, so reviews don't re-derive them>
- **Severity calibration:** <what Critical means in your domain, e.g. "any wrong financial figure">
- **Verification commands:** <test suites, benchmarks, lint/type checks the chair should
  recommend per area>
- **Scope conventions:** <default review scope, e.g. diff-only vs module>
- **Additional specialists:** <any custom advisors added to the swarm (e.g. security,
  compliance) and their ID prefixes>
- **Report destination:** <where board reports get filed, if kept>
