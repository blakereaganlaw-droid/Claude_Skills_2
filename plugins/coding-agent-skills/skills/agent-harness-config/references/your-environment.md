# Your Claude Code environment (sanitized template)

Fill in your project's real setup. Keep anything sensitive in `your-environment.private.md`
(git-ignored).

- **Test command(s) to allow-list:** <e.g. `npm test`, `pytest -q`>
- **Lint / format command(s):** <e.g. `npm run lint`, `ruff check .`, `black .`>
- **Build command(s):** <e.g. `make build`>
- **SessionStart bootstrap:** <what a fresh session must run first, e.g. `make bootstrap`>
- **Behaviors to automate (event → action):**
  - <e.g. PostToolUse on Edit → run the formatter on the changed file>
  - <e.g. SessionStart → install deps and fetch test fixtures>
- **MCP servers you use:** <name → command/args>
- **Commands to keep in `ask`/`deny`:** <e.g. `git push`, deployment, `rm`>
- **Secrets handling:** <where secrets live; never commit them>
