# Your prompt-engineering environment (sanitized template)

Fill in your standing parameters so the intake gate starts from facts. Keep anything
sensitive in `your-environment.private.md` (git-ignored).

- **Default target model(s) and context budget:** <model, window, output ceiling>
- **Deployment surfaces:** <agent harness / API / chat UI / skill file / GPT-style config>
- **Ingestion constraints:** <file-naming rules, size limits, formats your pipeline accepts>
- **House terminology the artifacts must use:** <your domain vocabulary>
- **Standing risks to always check:** <e.g. prompts that touch financial data must state
  identifier-fidelity and money-as-cents rules>
- **Authorization convention:** <how you say "go" — e.g. "approved", "execute">
