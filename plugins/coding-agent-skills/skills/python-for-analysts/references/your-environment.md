# Your Python environment (sanitized template)

Fill in your project's real setup. Keep anything sensitive — real file paths with client names,
sample data rows, connection strings, credentials — in `your-environment.private.md` (git-ignored).
Commit only sanitized, structural notes here.

- **Python version:** <e.g. 3.11 — confirm with `python --version`>
- **Package manager / lockfile:** <pip + requirements.txt | uv | Poetry | conda>
- **How to build the environment from scratch:** <e.g. `python -m venv .venv && pip install -r requirements.txt`>
- **Core libraries you rely on:** <e.g. pandas, numpy, openpyxl, requests>
- **Where input data lives:** <sanitized folder pattern, e.g. `data/raw/<source>/<yyyy-mm>/`>
- **Where outputs go:** <e.g. `data/processed/`, a shared drive, a database table>
- **How scripts get run:** <manually | cron | Task Scheduler | Airflow | Claude Code hook>
- **Notebook vs script convention:** <e.g. explore in `notebooks/`, promote stable logic to `src/`>
- **Secrets handling:** <where credentials live, e.g. env vars / a secrets manager — never in code or git>
- **House style:** <formatter/linter, e.g. black + ruff; naming conventions>
