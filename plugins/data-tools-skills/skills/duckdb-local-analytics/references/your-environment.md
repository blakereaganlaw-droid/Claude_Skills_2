# Your DuckDB environment (sanitized template)

Fill in your setup. Data files never go in git — reference their locations only.

- **Standard datasets:** <dataset → path/glob → format (csv/parquet) → refresh cadence>
- **Parquet conversions you maintain:** <which big CSVs you keep converted>
- **Persistent .duckdb files:** <which analyses keep a database file and where>
- **Recurring analysis scripts:** <script → what it produces → when it runs>
- **Memory/temp settings:** <memory_limit, temp_directory on your machine>
- **Handoff conventions:** <parquet between scripts, csv/xlsx to humans>
