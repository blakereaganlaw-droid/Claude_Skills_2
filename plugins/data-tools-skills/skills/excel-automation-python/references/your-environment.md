# Your Excel automation environment (sanitized template)

Fill in your setup. Real report files belong in `references/*.local.*` (git-ignored).

- **Scripted reports:** <report → script → schedule>
- **Templates:** <template file per report; who maintains styling>
- **Sheet/column contracts:** <sheet names and column orders scripts rely on>
- **House number formats:** <e.g. money "#,##0.00", dates "yyyy-mm-dd">
- **Engine choices:** <openpyxl vs xlsxwriter per report and why>
- **Verification checks per report:** <totals, row counts, format spot checks>
- **Where outputs go:** <folder/naming convention, e.g. Reports/2026-07/name_YYYYMMDD.xlsx>
