# Your PDF documents (sanitized template)

Fill in per recurring document type. Real PDFs stay in `references/*.local.*` (git-ignored).

Per document type:
- **Document:** <e.g. Bank X monthly statement, Supplier Y invoice>
- **Native or scanned:** <text layer present?>
- **Recipe:** <tool + flavor/strategy + bbox/columns that work>
- **Structure quirks:** <repeated headers, section headings, continuation lines>
- **Validation totals:** <opening/closing balance, invoice total, stated counts>
- **Change history:** <when the layout last changed and what broke>
