# Your API conventions (sanitized template)

- **Auth per client type:** <sessions for X, JWT for Y; token lifetimes>
- **Error shape:** <the JSON shape and status-code map in force>
- **Pagination defaults:** <default limit, cap, offset vs cursor>
- **Naming conventions:** <URL style, snake/camel in JSON>
- **Versioning policy:** <none / URL prefix / header — and when it applies>
- **Rate limiting / CORS:** <what's configured and where>
- **OpenAPI consumers:** <who generates clients from /docs — keep it honest for them>
