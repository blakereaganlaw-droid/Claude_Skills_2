# Your frontend setup (sanitized template)

- **Surface map:** <which pages/apps are htmx vs React vs hybrid, and why>
- **CSS approach:** <Tailwind / CSS modules / other — the one you picked>
- **Component conventions:** <naming, folder shape, shared-component rules>
- **Type derivation:** <openapi-typescript / hand-mirrored / none>
- **State conventions:** <query key scheme; what (if anything) lives in a store>
- **Accessibility bar:** <the floor you enforce and how it's checked>
- **Toolchain deviations:** <any non-stock Vite config, with the reason>
